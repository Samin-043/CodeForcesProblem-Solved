#include<bits/stdc++.h>
#include<string.h>
using namespace std;

int main()
{
    char s[110],t[110],p[110];

    int n,flag;

    while((scanf("%d",&n))!=EOF)
    {
        while(n>0)
        {
            scanf("%s%s%s",s,t,p);
            int len;

            len=strlen(p);

            //flag=0;

            for(int i=0;p[i]!='\0';i++)
            {
                if(strcmp(s,t)==0)
                {
                    //flag=1;
                    break;
                }

                char m[110],n[110];

                else
                {

                }

            }


        }

    }

    return 0;

}
