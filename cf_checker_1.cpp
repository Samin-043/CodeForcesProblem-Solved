#include<bits/stdc++.h>

int main()
{
    long long int i,a,b;
    scanf("%lld",&a);
    for(i=0;i<a;i++)
    {
        scanf("%lld",&b);
        printf("%lld ",b);
    }
    return 0;
}
